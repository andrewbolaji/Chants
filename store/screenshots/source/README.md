# Final-candidate source captures

Place clean, unframed screenshots from the exact release candidate here only after the production walkthrough passes:

- `ios/01-stage.png` through `ios/05-songbook.png`
- `android/01-stage.png` through `android/05-songbook.png`

These inputs must remain truthful platform captures. The reusable `../frame.html` adds only the approved headline, deck, index, and Chants FC presentation around the real interface. It must never replace, redraw, retouch, or fabricate app content.

Do not use development screenshots, personal accounts, operator controls, fake engagement, maintenance states, or rights-sensitive media. The final rendered outputs belong in `../ios/` and `../android/` at the paths named in `../manifest.json`.
