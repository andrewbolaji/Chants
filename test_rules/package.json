{
  "name": "chants-rules-tests",
  "private": true,
  "scripts": {
    "test": "mocha --require ts-node/register --timeout 10000 --exit '*.test.ts'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.0",
    "firebase-admin": "^13.0.0",
    "mocha": "^11.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0",
    "@types/mocha": "^10.0.0"
  }
}
