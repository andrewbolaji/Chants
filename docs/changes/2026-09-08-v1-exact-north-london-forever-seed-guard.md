# V1 exact North London Forever seed guard

## Outcome

The release candidate now has a create-only production seed path for the reviewed `arsenal-north-london-forever` document. The guard refuses broad Arsenal reconciliation, dirty or different source, stale operational control, target or inventory drift, a changed plan, an unsafe private file, repeat apply, and ambiguous success without exact readback.

Production remains closed at generation 11 maintenance with destructive workers false, 192 total chants, and 12 Arsenal chants. This record proves the source and local guard only. It does not claim a production read, write, opening, walkthrough, or observation.

## Implementation

- Added a canonical plan format bound to the reviewed Git SHA, Arsenal source-file hash, target create projection, exact existing Arsenal source-owned projections, aggregate counts, generation-11 maintenance baseline, and required generation-12 core state with workers false.
- Added an owner-only plan and credential boundary that rejects symlinks, permissive modes, path substitution, oversized JSON, another Firebase project, emulator redirection, and a dirty or different checkout.
- Added one transactional apply path. It re-reads control, global count, target absence, the complete Arsenal inventory, and any normalized same-title collision before offering only `transaction.create` for the planned target.
- Added exact post-create aggregate, team-inventory, target-content, and control readback. A lost acknowledgement can report the target as observed only after that same proof passes. A normal repeat stops before writing.
- Updated the runbook and launch command center so the approved window cannot be mistaken for an already open or operator-runnable seed.
- Rebound the prepared store packet to every approved product-source change by path and SHA-256. All release screenshots remain pending because the candidate appearance and content changed.

## Devil's-advocate result

A careless operator cannot add scope during apply because club and chant arguments are absent there. An impatient operator cannot rewrite the target because Firestore receives a create operation, not set or update. Concurrent target creation, generation movement, another Arsenal row, a changed existing projection, source edits, a substituted credential, access denial, and transport ambiguity all stop or require exact readback. No failure path creates a replacement plan, widens to the established club writer, or advances operational control.

## Local verification

- Thirteen exact-seed guard and CLI cases pass; the complete seed suite reports 87 passing and TypeScript reports no issue.
- All 542 Flutter tests pass and `flutter analyze lib test` reports no issue.
- Functions compile and report 230 passing with 24 emulator-only cases pending.
- Six crest tests, 20 store-packet tests, 11 iOS startup tests, 37 guide, readiness, policy, and landing tests, launch services, native project, project memory, writing style, and project-governance regressions pass.
- Initial exact-head CI run `34198061885` passed all seven other jobs and exposed five stale Linux visual references across four golden cases. The exact failed-run artifact was downloaded, all five actual images were inspected, and only the evidenced Linux references plus platform selection were corrected. The 14 affected cases pass locally and scoped analysis remains clean. Replacement exact-head CI is still required.

## Remaining gate

The single consolidated Claude review is complete. Amend and push the accepted corrections into the one clean closure head, complete replacement exact-head CI, then run fresh private preflight. Only after Andrew confirms the paired phone is ready may the approved generation-12 window, exact create, named walkthrough, 30-minute observation, and mandatory generation-13 closure begin.
