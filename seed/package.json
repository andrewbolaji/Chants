{
  "name": "chants-seed",
  "private": true,
  "scripts": {
    "seed": "ts-node seed.ts",
    "seed:club": "ts-node seed.ts",
    "test": "mocha --require ts-node/register --timeout 10000 --exit 'validate.test.ts'"
  },
  "dependencies": {
    "firebase-admin": "^13.0.0"
  },
  "devDependencies": {
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0",
    "mocha": "^11.0.0",
    "@types/mocha": "^10.0.0"
  }
}
